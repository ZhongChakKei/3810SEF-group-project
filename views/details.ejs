<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Player Details - Liverpool FC</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f5f5f5;
    }
    .header {
      background: #c8102e;
      color: white;
      padding: 20px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    .header-content {
      max-width: 900px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .back-link {
      color: white;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 5px;
    }
    .back-link:hover {
      text-decoration: underline;
    }
    .container {
      max-width: 900px;
      margin: 30px auto;
      padding: 0 20px;
    }
    .player-header {
      background: white;
      border-radius: 8px;
      padding: 30px;
      margin-bottom: 20px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
      display: flex;
      justify-content: space-between;
      align-items: start;
    }
    .player-info h1 {
      color: #c8102e;
      font-size: 32px;
      margin-bottom: 10px;
    }
    .player-meta {
      display: flex;
      gap: 20px;
      flex-wrap: wrap;
      margin-top: 15px;
    }
    .meta-item {
      display: flex;
      flex-direction: column;
    }
    .meta-label {
      font-size: 12px;
      color: #999;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .meta-value {
      font-size: 18px;
      font-weight: 600;
      color: #333;
      margin-top: 5px;
    }
    .position-badge {
      display: inline-block;
      padding: 6px 12px;
      border-radius: 5px;
      font-size: 14px;
      font-weight: 500;
      margin-top: 5px;
    }
    .pos-goalkeeper { background: #ffeb3b; color: #000; }
    .pos-defender { background: #4caf50; color: white; }
    .pos-midfielder { background: #2196f3; color: white; }
    .pos-forward { background: #f44336; color: white; }
    .edit-btn {
      background: #00b2a9;
      color: white;
      padding: 10px 20px;
      border-radius: 5px;
      text-decoration: none;
      font-weight: 500;
    }
    .edit-btn:hover {
      background: #009688;
    }
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 20px;
      margin-bottom: 20px;
    }
    .stat-card {
      background: white;
      border-radius: 8px;
      padding: 25px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
      text-align: center;
    }
    .stat-value {
      font-size: 36px;
      font-weight: bold;
      color: #c8102e;
      margin-bottom: 5px;
    }
    .stat-label {
      font-size: 14px;
      color: #666;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .info-card {
      background: white;
      border-radius: 8px;
      padding: 25px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    .info-card h2 {
      color: #333;
      font-size: 20px;
      margin-bottom: 20px;
      padding-bottom: 10px;
      border-bottom: 2px solid #c8102e;
    }
    .info-row {
      display: flex;
      padding: 12px 0;
      border-bottom: 1px solid #eee;
    }
    .info-row:last-child {
      border-bottom: none;
    }
    .info-label {
      flex: 0 0 180px;
      font-weight: 600;
      color: #666;
    }
    .info-value {
      color: #333;
    }
    .tags {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }
    .tag {
      background: #e3f2fd;
      color: #1976d2;
      padding: 4px 12px;
      border-radius: 15px;
      font-size: 13px;
    }
  </style>
</head>
<body>
  <div class="header">
    <div class="header-content">
      <h1>⚽ Player Details</h1>
      <a href="/find" class="back-link">← Back to List</a>
    </div>
  </div>

  <div class="container">
    <div class="player-header">
      <div class="player-info">
        <h1><%= player.name %></h1>
        <div class="player-meta">
          <div class="meta-item">
            <span class="meta-label">Squad Number</span>
            <span class="meta-value"><%= player.squadNumber || 'N/A' %></span>
          </div>
          <div class="meta-item">
            <span class="meta-label">Position</span>
            <span class="position-badge pos-<%= player.position.toLowerCase() %>">
              <%= player.position %>
            </span>
          </div>
          <div class="meta-item">
            <span class="meta-label">Nationality</span>
            <span class="meta-value"><%= player.nationality || 'N/A' %></span>
          </div>
        </div>
      </div>
      <a href="/edit?_id=<%= player._id %>" class="edit-btn">Edit Player</a>
    </div>

    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-value"><%= player.stats ? player.stats.appearances : 0 %></div>
        <div class="stat-label">Appearances</div>
      </div>
      <div class="stat-card">
        <div class="stat-value"><%= player.stats ? player.stats.goals : 0 %></div>
        <div class="stat-label">Goals</div>
      </div>
      <div class="stat-card">
        <div class="stat-value"><%= player.stats ? player.stats.assists : 0 %></div>
        <div class="stat-label">Assists</div>
      </div>
      <div class="stat-card">
        <div class="stat-value"><%= player.stats ? (player.stats.minutes / 90).toFixed(1) : 0 %></div>
        <div class="stat-label">90s Played</div>
      </div>
    </div>

    <div class="info-card">
      <h2>Additional Information</h2>
      
      <div class="info-row">
        <div class="info-label">Full Name</div>
        <div class="info-value"><%= player.name %></div>
      </div>

      <div class="info-row">
        <div class="info-label">Height</div>
        <div class="info-value"><%= player.heightCm ? player.heightCm + ' cm' : 'N/A' %></div>
      </div>

      <div class="info-row">
        <div class="info-label">Date of Birth</div>
        <div class="info-value">
          <%= player.dateOfBirth ? new Date(player.dateOfBirth).toLocaleDateString('en-GB', { 
            year: 'numeric', month: 'long', day: 'numeric' 
          }) : 'N/A' %>
        </div>
      </div>

      <% if (player.tags && player.tags.length > 0) { %>
        <div class="info-row">
          <div class="info-label">Tags</div>
          <div class="info-value">
            <div class="tags">
              <% player.tags.forEach(tag => { %>
                <span class="tag"><%= tag %></span>
              <% }) %>
            </div>
          </div>
        </div>
      <% } %>

      <div class="info-row">
        <div class="info-label">Total Minutes</div>
        <div class="info-value"><%= player.stats ? player.stats.minutes.toLocaleString() : 0 %> minutes</div>
      </div>

      <% if (player.stats && player.stats.appearances > 0) { %>
        <div class="info-row">
          <div class="info-label">Goals per Game</div>
          <div class="info-value"><%= (player.stats.goals / player.stats.appearances).toFixed(2) %></div>
        </div>

        <div class="info-row">
          <div class="info-label">Minutes per Goal</div>
          <div class="info-value">
            <%= player.stats.goals > 0 ? Math.round(player.stats.minutes / player.stats.goals) : 'N/A' %>
          </div>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
