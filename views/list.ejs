<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Players - Liverpool FC</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f5f5f5;
    }
    .header {
      background: #c8102e;
      color: white;
      padding: 20px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    .header-content {
      max-width: 1200px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .header h1 {
      font-size: 24px;
    }
    .user-info {
      display: flex;
      align-items: center;
      gap: 15px;
    }
    .avatar {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background: white;
    }
    .logout-btn {
      background: #00b2a9;
      color: white;
      border: none;
      padding: 8px 16px;
      border-radius: 5px;
      cursor: pointer;
      font-size: 14px;
    }
    .logout-btn:hover {
      background: #009688;
    }
    .container {
      max-width: 1200px;
      margin: 30px auto;
      padding: 0 20px;
    }
    .toolbar {
      background: white;
      padding: 20px;
      border-radius: 8px;
      margin-bottom: 20px;
      display: flex;
      gap: 15px;
      align-items: center;
      flex-wrap: wrap;
    }
    .search-box {
      flex: 1;
      min-width: 200px;
      display: flex;
      gap: 10px;
    }
    .search-box input {
      flex: 1;
      padding: 10px;
      border: 1px solid #ddd;
      border-radius: 5px;
      font-size: 14px;
    }
    .btn {
      padding: 10px 20px;
      background: #c8102e;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      text-decoration: none;
      font-size: 14px;
      display: inline-block;
    }
    .btn:hover {
      background: #a00d26;
    }
    .btn-secondary {
      background: #00b2a9;
    }
    .btn-secondary:hover {
      background: #009688;
    }
    .card {
      background: white;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    table {
      width: 100%;
      border-collapse: collapse;
    }
    th, td {
      padding: 15px;
      text-align: left;
      border-bottom: 1px solid #eee;
    }
    th {
      background: #f9f9f9;
      font-weight: 600;
      color: #333;
    }
    tr:hover {
      background: #f5f5f5;
    }
    .position-badge {
      display: inline-block;
      padding: 4px 8px;
      border-radius: 4px;
      font-size: 12px;
      font-weight: 500;
    }
    .pos-goalkeeper { background: #ffeb3b; color: #000; }
    .pos-defender { background: #4caf50; color: white; }
    .pos-midfielder { background: #2196f3; color: white; }
    .pos-forward { background: #f44336; color: white; }
    .stats {
      font-size: 12px;
      color: #666;
    }
    .actions {
      display: flex;
      gap: 10px;
    }
    .actions a {
      color: #c8102e;
      text-decoration: none;
      font-weight: 500;
    }
    .actions a:hover {
      text-decoration: underline;
    }
    .empty-state {
      padding: 60px 20px;
      text-align: center;
      color: #999;
    }
    .empty-state .icon {
      font-size: 64px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="header">
    <div class="header-content">
      <h1>⚽ Liverpool FC - Players</h1>
      <div class="user-info">
        <% if (user && user.avatarUrl) { %>
          <img src="<%= user.avatarUrl %>" alt="Avatar" class="avatar">
        <% } %>
        <span><%= user ? user.displayName : 'Guest' %></span>
        <form action="/logout" method="POST" style="display: inline;">
          <button type="submit" class="logout-btn">Logout</button>
        </form>
      </div>
    </div>
  </div>

  <div class="container">
    <div class="toolbar">
      <form class="search-box" action="/find" method="GET">
        <input type="text" name="q" placeholder="Search players by name, position, nationality, or tags..." value="<%= searchQuery %>">
        <button type="submit" class="btn btn-secondary">Search</button>
      </form>
      <a href="/create" class="btn">+ Add Player</a>
    </div>

    <div class="card">
      <% if (players.length === 0) { %>
        <div class="empty-state">
          <div class="icon">⚽</div>
          <h2>No players found</h2>
          <p><% if (searchQuery) { %>Try a different search term<% } else { %>Start by adding your first player<% } %></p>
        </div>
      <% } else { %>
        <table>
          <thead>
            <tr>
              <th>#</th>
              <th>Name</th>
              <th>Position</th>
              <th>Nationality</th>
              <th>Stats</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% players.forEach(player => { %>
              <tr>
                <td><strong><%= player.squadNumber || '-' %></strong></td>
                <td><%= player.name %></td>
                <td>
                  <span class="position-badge pos-<%= player.position.toLowerCase() %>">
                    <%= player.position %>
                  </span>
                </td>
                <td><%= player.nationality || '-' %></td>
                <td class="stats">
                  <%= player.stats ? player.stats.appearances : 0 %> apps | 
                  <%= player.stats ? player.stats.goals : 0 %> goals | 
                  <%= player.stats ? player.stats.assists : 0 %> assists
                </td>
                <td>
                  <div class="actions">
                    <a href="/details?_id=<%= player._id %>">View</a>
                    <a href="/edit?_id=<%= player._id %>">Edit</a>
                  </div>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>
  </div>
</body>
</html>
