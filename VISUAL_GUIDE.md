# 🗺️ Visual Project Guide

This document provides visual representations of the project structure and flows.

---

## 📊 Project Overview Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                    LIVERPOOL FC MANAGEMENT SYSTEM                    │
│                                                                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │   Browser    │  │  API Client  │  │    Admin     │             │
│  │   (Users)    │  │ (External)   │  │  (Future)    │             │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘             │
│         │                  │                  │                      │
│         │ HTTP/HTTPS       │ REST API         │                      │
│         ▼                  ▼                  ▼                      │
│  ┌──────────────────────────────────────────────────┐              │
│  │            EXPRESS.JS SERVER (server.js)          │              │
│  │                                                    │              │
│  │  ┌──────────────────────────────────────────┐   │              │
│  │  │         MIDDLEWARE STACK                  │   │              │
│  │  │  1. Session Management                    │   │              │
│  │  │  2. Passport Initialization               │   │              │
│  │  │  3. Form Parsing (express-formidable)     │   │              │
│  │  │  4. User Context Injection                │   │              │
│  │  └──────────────────────────────────────────┘   │              │
│  │                                                    │              │
│  │  ┌─────────────────┐  ┌─────────────────┐       │              │
│  │  │  🔒 MVC ROUTES   │  │  🌐 REST API     │       │              │
│  │  │  (Protected)     │  │  (Public)        │       │              │
│  │  │                  │  │                  │       │              │
│  │  │  /find          │  │  GET /api/...    │       │              │
│  │  │  /create        │  │  POST /api/...   │       │              │
│  │  │  /edit          │  │  PUT /api/...    │       │              │
│  │  │  /update        │  │  DELETE /api/... │       │              │
│  │  │  /details       │  │                  │       │              │
│  │  └────────┬────────┘  └────────┬────────┘       │              │
│  └───────────┼──────────────────────┼───────────────┘              │
│              │                      │                                │
│              ▼                      ▼                                │
│  ┌──────────────────────────────────────────────────┐              │
│  │         AUTHENTICATION (auth/passport.js)         │              │
│  │  ┌────────────────────────────────────────────┐  │              │
│  │  │  Google OAuth Strategy                      │  │              │
│  │  │  - Verify user with Google                 │  │              │
│  │  │  - Create/Update user in DB                │  │              │
│  │  │  - Store in session                        │  │              │
│  │  └────────────────────────────────────────────┘  │              │
│  └──────────────────────┬───────────────────────────┘              │
│                         │                                            │
│                         ▼                                            │
│  ┌──────────────────────────────────────────────────┐              │
│  │         DATA LAYER (MongoDB)                      │              │
│  │  ┌──────────────┐       ┌──────────────┐         │              │
│  │  │   players    │       │    users     │         │              │
│  │  │ collection   │       │  collection  │         │              │
│  │  │              │       │              │         │              │
│  │  │ - name       │       │ - provider   │         │              │
│  │  │ - position   │       │ - providerId │         │              │
│  │  │ - stats      │       │ - email      │         │              │
│  │  │ - tags       │       │ - displayName│         │              │
│  │  └──────────────┘       └──────────────┘         │              │
│  └────────────────────────────────────────────────────┘            │
│                                                                       │
│  ┌──────────────────────────────────────────────────┐              │
│  │         VIEW LAYER (EJS Templates)                │              │
│  │  login.ejs  |  list.ejs  |  create.ejs          │              │
│  │  edit.ejs   |  details.ejs  |  info.ejs         │              │
│  └──────────────────────────────────────────────────┘              │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow Diagrams

### MVC Request Flow (Protected)

```
User                Server              Auth              DB              View
 │                    │                  │                 │                │
 │   GET /find        │                  │                 │                │
 ├───────────────────>│                  │                 │                │
 │                    │                  │                 │                │
 │                    │  Check session   │                 │                │
 │                    ├─────────────────>│                 │                │
 │                    │                  │                 │                │
 │                    │  req.user exists?│                 │                │
 │                    │<─────────────────┤                 │                │
 │                    │                  │                 │                │
 │                    │  [If No]         │                 │                │
 │  Redirect /login   │  Store next URL  │                 │                │
 │<───────────────────┤  & redirect      │                 │                │
 │                    │                  │                 │                │
 │   GET /login       │                  │                 │                │
 ├───────────────────>│                  │                 │                │
 │                    │                  │                 │                │
 │                    │                  │                 │  Render login  │
 │                    │                  │                 ├───────────────>│
 │   login.ejs HTML   │                  │                 │                │
 │<────────────────────────────────────────────────────────────────────────┤
 │                    │                  │                 │                │
 │  Click "Google"    │                  │                 │                │
 ├───────────────────>│                  │                 │                │
 │                    │  Redirect Google │                 │                │
 │  [OAuth Flow]      │<─────────────────┤                 │                │
 │<───────────────────┤                  │                 │                │
 │                    │                  │                 │                │
 │  [After Google]    │                  │                 │                │
 │  Callback          │                  │                 │                │
 ├───────────────────>│                  │                 │                │
 │                    │  Verify & upsert │                 │                │
 │                    ├─────────────────>│  Find/Update    │                │
 │                    │                  ├────────────────>│                │
 │                    │                  │  User document  │                │
 │                    │                  │<────────────────┤                │
 │                    │  Store in session│                 │                │
 │                    │<─────────────────┤                 │                │
 │                    │                  │                 │                │
 │  Redirect /find    │                  │                 │                │
 │<───────────────────┤                  │                 │                │
 │                    │                  │                 │                │
 │   GET /find        │                  │                 │                │
 ├───────────────────>│                  │                 │                │
 │                    │  [Now logged in] │                 │                │
 │                    │  Query players   │                 │                │
 │                    ├─────────────────────────────────>│                │
 │                    │                  │  Players array  │                │
 │                    │<─────────────────────────────────┤                │
 │                    │                  │                 │                │
 │                    │                  │                 │  Render list   │
 │                    │                  │                 ├───────────────>│
 │   list.ejs HTML    │                  │                 │                │
 │<────────────────────────────────────────────────────────────────────────┤
```

### REST API Request Flow (Public)

```
Client              Server                           DB
  │                   │                               │
  │  GET /api/players │                               │
  ├──────────────────>│                               │
  │                   │  Parse query params           │
  │                   │  (search, position)           │
  │                   │                               │
  │                   │  Build MongoDB query          │
  │                   │                               │
  │                   │  db.collection('players')     │
  │                   │  .find(query).toArray()       │
  │                   ├──────────────────────────────>│
  │                   │                               │
  │                   │  Players array                │
  │                   │<──────────────────────────────┤
  │                   │                               │
  │  JSON response    │  res.json(players)            │
  │<──────────────────┤                               │
  │                   │                               │
  │                   │                               │
  │  POST /api/players│                               │
  │  (with form data) │                               │
  ├──────────────────>│                               │
  │                   │  Parse form data              │
  │                   │  (express-formidable)         │
  │                   │                               │
  │                   │  Validate required fields     │
  │                   │  (name)                       │
  │                   │                               │
  │                   │  Build player document        │
  │                   │                               │
  │                   │  db.collection('players')     │
  │                   │  .insertOne(player)           │
  │                   ├──────────────────────────────>│
  │                   │                               │
  │                   │  { insertedId }               │
  │                   │<──────────────────────────────┤
  │                   │                               │
  │  JSON (201)       │  res.status(201)              │
  │  New player       │  .json(player)                │
  │<──────────────────┤                               │
```

---

## 📂 File Dependency Graph

```
server.js (Main Entry)
  │
  ├─> .env (Environment Variables)
  │     └─> MONGODB_URI
  │     └─> GOOGLE_CLIENT_ID
  │     └─> GOOGLE_CLIENT_SECRET
  │     └─> SESSION_SECRET
  │
  ├─> auth/passport.js (OAuth Configuration)
  │     └─> GoogleStrategy
  │     └─> serializeUser
  │     └─> deserializeUser
  │     └─> isLoggedIn middleware
  │
  ├─> views/*.ejs (Templates)
  │     ├─> login.ejs
  │     ├─> list.ejs
  │     ├─> create.ejs
  │     ├─> edit.ejs
  │     ├─> details.ejs
  │     └─> info.ejs
  │
  └─> MongoDB Connection
        ├─> players collection
        └─> users collection
```

---

## 🔐 Authentication Flow Diagram

```
┌──────────┐
│  START   │
└────┬─────┘
     │
     ▼
┌─────────────────────┐
│ User visits /find   │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐      YES    ┌──────────────────┐
│ req.user exists?    ├────────────>│ Show player list │
└────┬────────────────┘             └──────────────────┘
     │ NO
     ▼
┌─────────────────────┐
│ Store next URL      │
│ Redirect to /login  │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ Show login page     │
│ "Sign in w/ Google" │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ User clicks button  │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ Redirect to Google  │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ Google auth page    │
│ User logs in        │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ User grants access  │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ Google redirects    │
│ to /callback        │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ Passport verifies   │
│ with Google         │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ Find/create user    │
│ in MongoDB          │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ Store user in       │
│ session (serialize) │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ Redirect to saved   │
│ URL or /find        │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ User sees player    │
│ list (authenticated)│
└─────────────────────┘
```

---

## 🗄️ Database Schema Visual

```
┌─────────────────────────────────────────┐
│         players collection               │
├─────────────────────────────────────────┤
│  _id: ObjectId (auto)                   │
│  name: String (required) ───────────────┼──> "Mohamed Salah"
│  position: String ──────────────────────┼──> "Forward"
│  squadNumber: Number ───────────────────┼──> 11
│  nationality: String ───────────────────┼──> "Egypt"
│  heightCm: Number ──────────────────────┼──> 175
│  dateOfBirth: Date ─────────────────────┼──> ISODate("1992-06-15")
│  stats: {                               │
│    appearances: Number ────────────────┼──> 250
│    goals: Number ──────────────────────┼──> 180
│    assists: Number ────────────────────┼──> 70
│    minutes: Number ────────────────────┼──> 20000
│  }                                       │
│  tags: [String] ────────────────────────┼──> ["Captain", "Legend"]
│  createdAt: Date ───────────────────────┼──> ISODate("2023-01-01")
│  updatedAt: Date (optional) ────────────┼──> ISODate("2023-06-15")
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         users collection                 │
├─────────────────────────────────────────┤
│  _id: ObjectId (auto)                   │
│  provider: String ──────────────────────┼──> "google"
│  providerId: String ────────────────────┼──> "1234567890"
│  email: String ─────────────────────────┼──> "user@example.com"
│  displayName: String ───────────────────┼──> "John Doe"
│  avatarUrl: String ─────────────────────┼──> "https://..."
│  createdAt: Date ───────────────────────┼──> ISODate("2023-01-01")
│  lastLogin: Date ───────────────────────┼──> ISODate("2023-06-15")
└─────────────────────────────────────────┘
```

---

## 🎨 UI Flow Map

```
┌─────────────┐
│   login.ejs │ ──────────────────┐
└──────┬──────┘                   │
       │                          │
       │ Sign in with Google      │
       │                          │
       ▼                          │
┌─────────────┐                   │
│ [OAuth Flow]│                   │
└──────┬──────┘                   │
       │                          │ Not logged in
       │ Success                  │ redirects here
       │                          │
       ▼                          │
┌─────────────┐                   │
│   list.ejs  │<──────────────────┘
│ (Player     │
│  List)      │
└──┬────┬──┬──┘
   │    │  │
   │    │  └──────────────┐
   │    │                 │
   │    │ View details    │ Search/Filter
   │    │                 │
   │    ▼                 │
   │  ┌─────────────┐     │
   │  │ details.ejs │     │
   │  │ (Player     │     │
   │  │  Details)   │     │
   │  └──────┬──────┘     │
   │         │            │
   │         │ Edit       │
   │         │            │
   │         ▼            │
   │  ┌─────────────┐     │
   │  │   edit.ejs  │     │
   │  │ (Edit Form) │     │
   │  └──────┬──────┘     │
   │         │            │
   │         │ Submit     │
   │         │            │
   │         │ POST       │
   │         │ /update    │
   │         │            │
   │         └────────────┼───> Back to list
   │                      │
   │ Add Player           │
   │                      │
   ▼                      │
┌─────────────┐           │
│ create.ejs  │           │
│ (Add Form)  │           │
└──────┬──────┘           │
       │                  │
       │ Submit           │
       │                  │
       │ POST /create     │
       │                  │
       └──────────────────┘
```

---

## 📡 API Endpoint Map

```
REST API (/api)
│
├─> /players
│   │
│   ├─> GET              List all players
│   │   └─> ?search=x    Search players
│   │   └─> ?position=x  Filter by position
│   │
│   ├─> POST             Create new player
│   │
│   └─> /:id
│       │
│       ├─> GET          Get one player
│       ├─> PUT          Update player
│       └─> DELETE       Delete player
│
└─> [Future endpoints]
    ├─> /stats           Team statistics
    ├─> /positions       Position summary
    └─> /search          Advanced search
```

---

## 🔄 Session Lifecycle

```
┌─────────────────────────────────────────────────────────────┐
│                    SESSION LIFECYCLE                         │
└─────────────────────────────────────────────────────────────┘

1. INITIALIZATION
   ┌──────────────┐
   │ User visits  │
   │ any page     │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Express      │
   │ creates new  │
   │ session      │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Session ID   │
   │ stored in    │
   │ cookie       │
   └──────────────┘

2. AUTHENTICATION
   ┌──────────────┐
   │ OAuth        │
   │ completes    │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Passport     │
   │ serializes   │
   │ user         │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ User object  │
   │ stored in    │
   │ session      │
   └──────────────┘

3. SUBSEQUENT REQUESTS
   ┌──────────────┐
   │ User visits  │
   │ protected    │
   │ route        │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Session ID   │
   │ from cookie  │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Passport     │
   │ deserializes │
   │ user         │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ req.user     │
   │ populated    │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ isLoggedIn   │
   │ checks       │
   │ req.user     │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Access       │
   │ granted      │
   └──────────────┘

4. LOGOUT
   ┌──────────────┐
   │ POST /logout │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ req.logout() │
   │ called       │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Session      │
   │ destroyed    │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Cookie       │
   │ cleared      │
   └──────────────┘
```

---

## 🎯 Data Flow Summary

```
┌─────────────────────────────────────────────────────────────┐
│                     DATA FLOW OVERVIEW                       │
└─────────────────────────────────────────────────────────────┘

USER INPUT
    │
    ├─> Form Data (create/edit) ──> express-formidable
    │                                     │
    │                                     ▼
    │                              req.fields object
    │                                     │
    │                                     ▼
    │                              Handler function
    │                                     │
    │                                     ▼
    │                              Validation
    │                                     │
    │                                     ▼
    │                              MongoDB operation
    │                                     │
    │                                     ▼
    │                              Response (redirect/render)
    │
    └─> API Request (JSON) ──────> express-formidable
                                          │
                                          ▼
                                   req.fields object
                                          │
                                          ▼
                                   REST handler
                                          │
                                          ▼
                                   Validation
                                          │
                                          ▼
                                   MongoDB operation
                                          │
                                          ▼
                                   JSON response
```

---

## 🏗️ Component Interaction Matrix

```
┌─────────────┬─────────┬─────────┬─────────┬─────────┬─────────┐
│ Component   │ Express │ Passport│ MongoDB │ EJS     │ Client  │
├─────────────┼─────────┼─────────┼─────────┼─────────┼─────────┤
│ Express     │    -    │  Uses   │  Uses   │  Uses   │ Serves  │
│ Passport    │  Used by│    -    │  Uses   │    -    │  Auth   │
│ MongoDB     │  Used by│  Used by│    -    │    -    │    -    │
│ EJS         │  Used by│    -    │    -    │    -    │ Renders │
│ Client      │  Calls  │    -    │    -    │ Receives│    -    │
└─────────────┴─────────┴─────────┴─────────┴─────────┴─────────┘

Legend:
  -        = No direct interaction
  Uses     = Component actively uses another
  Used by  = Component is used by another
  Serves   = Serves content to
  Auth     = Authenticates
  Renders  = Renders templates
  Receives = Receives rendered HTML
  Calls    = Makes HTTP requests
```

---

## 📦 Module Loading Sequence

```
1. server.js starts
        │
        ├─> Load dotenv (.env)
        │
        ├─> Load dependencies
        │   ├─> express
        │   ├─> express-session
        │   ├─> passport
        │   ├─> express-formidable
        │   └─> mongodb
        │
        ├─> Load custom modules
        │   └─> auth/passport.js
        │
        ├─> Connect to MongoDB
        │   └─> Initialize collections
        │
        ├─> Setup Passport
        │   └─> Call setupPassport(db)
        │
        ├─> Configure middleware
        │   ├─> Session
        │   ├─> Passport init
        │   ├─> Passport session
        │   └─> Formidable
        │
        ├─> Define routes
        │   ├─> Auth routes
        │   ├─> MVC routes
        │   ├─> API routes
        │   └─> 404 handler
        │
        └─> Start listening (port 3000)
```

---

## 🎨 Color Coding Legend

Throughout the documentation:

- 🔴 **Red** - Liverpool FC primary color
- 🟢 **Green** - Success/Completed
- 🟡 **Yellow** - Warning/In Progress
- 🔵 **Blue** - Information
- ⚫ **Black** - Default/Neutral
- 🟣 **Purple** - Special/Premium features

---

**This visual guide complements the technical documentation. Refer to other docs for detailed implementation.**

**You'll Never Walk Alone!** 🔴⚽
